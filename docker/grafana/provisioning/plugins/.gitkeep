# Intentionally empty. Grafana logs an error on every start if this directory is
# absent from a mounted provisioning tree; an empty directory keeps the log clean
# so a real provisioning error is not lost among recurring noise.
